# Example configuration for the webserver
# at https://github.com/monopole/hello

namespace: default

#commonLabels:
#  prune-test: true

resources:
- pod-c.yaml
- pod-b.yaml

inventory:
  type: ConfigMap
  configMap:
    name: pod-inventory
    namespace: default
