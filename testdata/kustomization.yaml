# Example configuration for the webserver
# at https://github.com/monopole/hello

namespace: default

resources:
- pod-a.yaml
- pod-b.yaml

inventory:
  type: ConfigMap
  configMap:
    name: pod-inventory
    namespace: default
